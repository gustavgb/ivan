class Component {
  constructor (indentation, text, parent) {
    this.indentation = indentation
    this.text = text
    this.parent = parent
  }

  render () {
    return ''
  }
}

export default Component
