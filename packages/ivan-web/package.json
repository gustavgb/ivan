{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ivan-dev-server --src src",
    "build": "ivan build --src src"
  },
  "devDependencies": {
    "@gustavgb/ivan": "*",
    "@gustavgb/ivan-dev-server": "*"
  }
}
