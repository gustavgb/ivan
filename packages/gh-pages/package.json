{
  "name": "gh-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "ivan watch -s src",
    "build": "ivan build -s src",
    "start": "node tools/startDev.js"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "serve": "^10.1.1",
    "@gustavgb/ivan": "*"
  }
}
